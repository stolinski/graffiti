export type ThemeValues = {
  fg_light: string;
  fg_dark: string;
  bg_light: string;
  bg_dark: string;
  primary: string;
};

export type FontSettings = {
  min_ratio: number;
  max_ratio: number;
  min_font_size: number;
  max_font_size: number;
  min_viewport: number;
  max_viewport: number;
};
