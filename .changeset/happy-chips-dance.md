---
"@drop-in/graffiti": patch
---

Added chips component - small interactive tag/label elements for filters, categories, and selections. Supports icons, selected state (via `.selected` class or `aria-pressed`), disabled state, and a compact `.mini` variant.
