# Template Remediation Map

Date: 2026-03-12  
Dex task: `qbg6hxyo`

## Cross-Template Remediation Families

| Family                                | Baseline evidence                                                                                | Planned remediation                                                                                      | Target Phase 2 tasks                                                                           |
| ------------------------------------- | ------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Stack/cluster gap micro-tuning        | `--gap` appears 97 times inline (`xs/s/m/xl/0`)                                                  | Add class-level gap modifiers (or equivalent utility pack) so templates stop inlining `--gap` repeatedly | `clo1mys3`, consumed by `tdu48bfp`, `69pllbj5`, `bbduaimz`, `e60rs1hr`, `h805g4hk`, `wb3hpt6c` |
| Margin reset and micro-spacing debt   | `margin` appears 49 times; `margin: 0` appears 40 times                                          | Add margin reset utilities and compact spacing helpers (`m-0`, axis/edge helpers)                        | `clo1mys3`, consumed by all template refactors                                                 |
| Tag semantic drift from inline colors | `--tag-color` appears 21 times inline                                                            | Introduce semantic tag variants (`success`, `warning`, `error`, `info`, etc.) and migrate status tags    | `xy93mfpb`, consumed by `bbduaimz`, `e60rs1hr`, `69pllbj5`, `tdu48bfp`                         |
| Width and alignment overrides         | Repeated `align-self`, `align-items`, `width: 100%`, `max-width` declarations                    | Add utility coverage for self-alignment, full-width, and common max-width patterns                       | `clo1mys3`, `xy93mfpb`                                                                         |
| Form option row repetition            | Settings template repeats `display: inline-flex; align-items: center; gap: ...; cursor: pointer` | Add canonical form option row primitive for radio/checkbox preference controls                           | `xy93mfpb`, `h805g4hk`                                                                         |
| App-shell/chat token blob             | AI chat template has one large hardcoded token override plus shell/composer inline patching      | Add class-first app-shell/chat helpers and theme presets                                                 | `y621ub1t`, `wb3hpt6c`                                                                         |
| Linked-card composition inconsistency | Blog related-card links inline `text-decoration`, `color`, and zero-gap card stack               | Add linked-card variant/pattern with predictable hover/focus/readability behavior                        | `xy93mfpb`, `e60rs1hr`                                                                         |

## File-by-File Remediation Plan

| File                                          | Baseline debt signal                                 | Key quality concerns                                                                                                                         | Remediation approach                                                                                                                               | Target Phase 2 task(s)                         |
| --------------------------------------------- | ---------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| `src/routes/templates/+layout.svelte`         | 1 inline attr + 11 style directives                  | Shared shell contains one-off back-link styling; style directives are valid for theme controls but should not leak into template examples    | Remove inline back-link styling with class-first utility; keep directive-based theme controls scoped to docs behavior                              | `tdu48bfp`                                     |
| `src/routes/templates/+page.svelte`           | 39 attrs / 47 decls                                  | Repeated card internals (`stack` gaps, heading/body margin resets, CTA alignment) and inline tag colors                                      | Use shared card pattern + new utilities/variants for spacing, alignment, and category tags; keep AI chat card hidden until chat refactor passes    | `tdu48bfp`, `clo1mys3`, `xy93mfpb`             |
| `src/routes/templates/landing/+page.svelte`   | 77 attrs / 95 decls (largest debt)                   | Section internals rely on heavy one-off spacing and typography declarations; pricing/footer blocks duplicate margin and weight resets        | Shift to canonical section + card composition with utility classes for spacing/width/weights; replace inline status/category accents with variants | `69pllbj5`, `clo1mys3`, `xy93mfpb`             |
| `src/routes/templates/dashboard/+page.svelte` | 20 attrs / 24 decls                                  | Status tags fully inline-colored; shell spacing/borders and title margins patched inline; table status readability depends on inline colors  | Introduce semantic status tag variants and app-shell/sidebar helpers; normalize KPI/table sections to class-first patterns                         | `bbduaimz`, `xy93mfpb`, `y621ub1t`             |
| `src/routes/templates/blog/+page.svelte`      | 24 attrs / 38 decls                                  | TOC stickiness and related-card link treatments depend on inline declarations; callout colors are inline custom props                        | Add linked-card and callout variant patterns; move sticky/layout concerns to layout utility guidance; preserve article readability defaults        | `e60rs1hr`, `xy93mfpb`, `y621ub1t`             |
| `src/routes/templates/settings/+page.svelte`  | 42 attrs / 67 decls (second-highest debt)            | Large amount of form micro-layout inline styles (`margin`, option rows, separators, heading color); repeated section heading spacing patches | Add canonical form row and separator utilities; normalize section scaffolding and action rows; keep semantics for labels/fieldsets/toggles intact  | `h805g4hk`, `clo1mys3`, `xy93mfpb`, `y621ub1t` |
| `src/routes/templates/ai-chat/+page.svelte`   | 28 attrs / 51 decls + hardcoded token override block | Theme overrides and shell behavior are brittle; repeated message/composer spacing overrides; template hidden from index due quality concerns | Replace token blob with class-driven preset, harden sidebar/thread/composer shell APIs, then re-enable template on index                           | `wb3hpt6c`, `y621ub1t`, `tdu48bfp`             |

## Sequencing

1. Build utility and variant foundation first: `clo1mys3` + `xy93mfpb`.
2. Harden shell APIs for app-like templates: `y621ub1t`.
3. Refactor templates in sequence: `tdu48bfp`, `69pllbj5`, `bbduaimz`, `e60rs1hr`, `h805g4hk`, `wb3hpt6c`.
4. Align LLM docs to post-refactor canonical patterns: `z9cgbu5y`.
5. Add automated regression checks: `yq0h5um7`.
6. Run final visual/mobile QA matrix and verification: `3kyhe7vs`.
