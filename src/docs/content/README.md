# Docs Content Source

This directory is the single authored source for runtime docs fan-out.

- Topic markdown lives in `src/docs/content/topics/*.md`
- Interactive demos stay in `src/docs/demos/*.svelte`

Validation:

- `npm run check:docs-content`

Current phase status:

- Runtime content graph scaffold is in place (`src/docs/content/runtime.js`)
- Topic migration is in progress route by route
