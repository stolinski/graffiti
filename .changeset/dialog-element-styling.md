---
"@drop-in/graffiti": minor
---

Added native `<dialog>` element styling with open/close animations, and a reusable `.close` button class. Demos use HTML invokers (`commandfor`/`command`) - no JavaScript required.
