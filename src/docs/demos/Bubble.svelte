<div class="stack" style="--chat-thread-padding: 0; --chat-thread-gap: var(--pad-s)">
  <div class="chat-thread box" style="--chat-thread-padding: var(--pad-s)">
    <div class="chat-row">
      <article class="chat-message bubble">
        Assistant bubble with the default surface and border tokens.
      </article>
    </div>

    <div class="chat-row self">
      <article
        class="chat-message bubble"
        style="--bubble-bg: var(--primary-1); --bubble-border: var(--primary-5)"
      >
        User bubble with inline CSS variable overrides only.
      </article>
    </div>

    <div class="chat-row">
      <article class="chat-message bubble">
        This example shows long wrapping behavior with
        supercalifragilisticexpialidociousrepeatwithoutspaces0123456789abcdefghijklmnopqrstuvwxyz
        and a long URL style token:
        https://example.com/some/really/long/path/that/keeps/going/without/breakpoints
      </article>
    </div>

    <div class="chat-row">
      <article class="chat-message bubble">
        <h5>Deployment status</h5>
        <p>
          Build finished in 43s, migrations passed, and the staging URL is
          ready for QA.
        </p>
      </article>
    </div>
  </div>
</div>
