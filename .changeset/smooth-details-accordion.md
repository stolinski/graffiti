---
"graffiti": minor
---

Added details/summary accordion component with smooth open/close animations using @starting-style and allow-discrete. Includes variants: `.bordered` (container with border), `.right` (right-aligned trigger), and `.minimal` (+/− toggle). Also added `button.minimal` for text-only buttons.
