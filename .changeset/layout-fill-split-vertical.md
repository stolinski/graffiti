---
"graffiti": minor
---

Added `.layout-sidebar.fill` modifier for full-height sidebars and `.split.vertical` for vertical space-between layouts. Also added `@property --layout-gap` to prevent gap values from cascading to nested layout elements.
